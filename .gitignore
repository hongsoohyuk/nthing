.claude/skills/git-commit-workflow/SKILL.md
.serena/cache/
.serena/project.local.yml

# macOS metadata
.DS_Store
**/.DS_Store

# Environment secrets
*.env
!*.env.example

# Credential drop folder (manual transfer between clones)
cre/

# Firebase service account (server admin private key — NEVER commit)
*-firebase-adminsdk-*.json
**/firebase-adminsdk*.json
server/secrets/
infra/firebase-adminsdk.json

# Firebase client config (API keys ship in the app binary, but kept out of git per repo convention)
mobile/android/app/google-services.json
mobile/ios/App/App/GoogleService-Info.plist